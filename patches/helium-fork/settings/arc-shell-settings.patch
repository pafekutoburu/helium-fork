Index: src/chrome/app/settings_strings.grdp
===================================================================
--- src.orig/chrome/app/settings_strings.grdp
+++ src/chrome/app/settings_strings.grdp
@@ -301,6 +301,18 @@
     <message name="IDS_SETTINGS_BROWSER_LAYOUT_VERTICAL" desc="Vertical option in the browser layout dropdown.">
       Vertical
     </message>
+    <message name="IDS_SETTINGS_ADDRESS_BAR_MODE" desc="Label for the dropdown that controls how the address bar is shown.">
+      Address bar
+    </message>
+    <message name="IDS_SETTINGS_ADDRESS_BAR_MODE_DOCKED" desc="Option in the address bar dropdown: the address bar is always on screen.">
+      Always visible
+    </message>
+    <message name="IDS_SETTINGS_ADDRESS_BAR_MODE_HIDDEN" desc="Option in the address bar dropdown: there is no address bar; the address lives in the sidebar.">
+      Hidden, in the sidebar
+    </message>
+    <message name="IDS_SETTINGS_ADDRESS_BAR_MODE_AUTO" desc="Option in the address bar dropdown: the address bar appears when the pointer reaches the top of the window.">
+      Show on hover
+    </message>
     <message name="IDS_SETTINGS_TAB_STRIP_RIGHT_ALIGN" desc="Label for the toggle that displays vertical tabs on the right side of the browser window.">
       Show vertical tabs on right side
     </message>
Index: src/chrome/browser/extensions/api/settings_private/prefs_util.cc
===================================================================
--- src.orig/chrome/browser/extensions/api/settings_private/prefs_util.cc
+++ src/chrome/browser/extensions/api/settings_private/prefs_util.cc
@@ -227,6 +227,10 @@ const PrefsUtil::TypedPrefMap& PrefsUtil
       settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHeliumMinimalLocationBar] =
       settings_api::PrefType::kBoolean;
+  // helium-fork: the settings page writes the address bar mode instead of
+  // the zen-mode boolean, which is now only read once during migration.
+  (*s_allowlist)[::prefs::kHeliumAddressBarMode] =
+      settings_api::PrefType::kNumber;
   (*s_allowlist)[::prefs::kHeliumZenMode] = settings_api::PrefType::kBoolean;
   (*s_allowlist)[::prefs::kHeliumZenModeSidebarPinned] =
       settings_api::PrefType::kBoolean;
Index: src/chrome/browser/resources/settings/appearance_page/appearance_page.ts
===================================================================
--- src.orig/chrome/browser/resources/settings/appearance_page/appearance_page.ts
+++ src/chrome/browser/resources/settings/appearance_page/appearance_page.ts
@@ -81,13 +81,14 @@ export enum SystemTheme {
   // </if>
 }
 
-// This must be kept in sync with the HeliumLayoutType enum in
-// chrome/browser/ui/helium/helium_layout_state_controller.h.
-enum BrowserLayout {
-  CLASSIC = 0,
-  COMPACT = 1,
-  VERTICAL = 2,
-  DYNAMIC = 3,
+// helium-fork: this must be kept in sync with the HeliumAddressBarMode enum
+// in chrome/browser/ui/helium/helium_layout_state_controller.h. The browser
+// layout itself is no longer a choice -- it is always the sidebar -- so the
+// HeliumLayoutType mirror that used to live here is gone.
+enum AddressBarMode {
+  DOCKED = 0,
+  HIDDEN = 1,
+  AUTO = 2,
 }
 
 function normalizePageVisibility(
@@ -141,7 +142,6 @@ export class SettingsAppearancePageEleme
       defaultZoom_: {type: Number},
 
       defaultFontSizePref_: {type: Object},
-      browserLayoutPref_: {type: Object},
       homepageIsNewTabPagePref_: {type: Object},
       homepagePref_: {type: Object},
       pinContextualTaskButtonPref_: {type: Object},
@@ -153,7 +153,6 @@ export class SettingsAppearancePageEleme
       sidePanelIsRightAlignedPref_: {type: Object},
       themeIdPref_: {type: Object},
       themePolicyColorPref_: {type: Object},
-      zenModePref_: {type: Object},
 
       // <if expr="is_linux">
       themeSystemThemePref_: {type: Object},
@@ -174,7 +173,7 @@ export class SettingsAppearancePageEleme
       showHoverCardImagesOption_: {type: Boolean},
       showManagedThemeDialog_: {type: Boolean},
       glassEffectOptions_: {type: Array},
-      browserLayoutOptions_: {type: Array},
+      addressBarModeOptions_: {type: Array},
       showVerticalTabsEnabled_: {type: Boolean},
       showNativeFrameMaterialsOption_: {type: Boolean},
       showGlassEffectEnabled_: {type: Boolean},
@@ -198,8 +197,6 @@ export class SettingsAppearancePageEleme
   protected accessor defaultZoom_: number = 1.0;
 
   // Mirrored prefs.
-  protected accessor browserLayoutPref_: PrefObject<number>|undefined =
-      undefined;
   protected accessor defaultFontSizePref_: PrefObject<number>|undefined =
       undefined;
   protected accessor homepageIsNewTabPagePref_: PrefObject<boolean>|undefined =
@@ -222,7 +219,6 @@ export class SettingsAppearancePageEleme
   protected accessor themeIdPref_: PrefObject<string>|undefined = undefined;
   protected accessor themePolicyColorPref_: PrefObject<number>|undefined =
       undefined;
-  protected accessor zenModePref_: PrefObject<boolean>|undefined = undefined;
 
   // <if expr="is_linux">
   protected accessor themeSystemThemePref_: PrefObject<number>|undefined =
@@ -264,22 +260,21 @@ export class SettingsAppearancePageEleme
     {value: 'true', name: loadTimeData.getString('glassEffectModern')},
  ];
 
-  protected accessor browserLayoutOptions_: DropdownMenuOptionList = [
-    {
-      value: BrowserLayout.CLASSIC,
-      name: loadTimeData.getString('browserLayoutClassic'),
-    },
+  // helium-fork: all three forms of the Arc shell. HIDDEN drops the band
+  // entirely and moves the address entry into the sidebar, so it is only
+  // offerable now that the sidebar has somewhere to put it.
+  protected accessor addressBarModeOptions_: DropdownMenuOptionList = [
     {
-      value: BrowserLayout.DYNAMIC,
-      name: loadTimeData.getString('browserLayoutDynamic'),
+      value: AddressBarMode.DOCKED,
+      name: loadTimeData.getString('addressBarModeDocked'),
     },
     {
-      value: BrowserLayout.COMPACT,
-      name: loadTimeData.getString('browserLayoutCompact'),
+      value: AddressBarMode.HIDDEN,
+      name: loadTimeData.getString('addressBarModeHidden'),
     },
     {
-      value: BrowserLayout.VERTICAL,
-      name: loadTimeData.getString('browserLayoutVertical'),
+      value: AddressBarMode.AUTO,
+      name: loadTimeData.getString('addressBarModeAuto'),
     },
   ];
 
@@ -353,7 +348,6 @@ export class SettingsAppearancePageEleme
 
     this.mirrorPrefs({
       'autogenerated.theme.policy.color': 'themePolicyColorPref_',
-      'helium.browser.layout': 'browserLayoutPref_',
       'browser.pin_contextual_task_button': 'pinContextualTaskButtonPref_',
       'browser.pin_split_tab_button': 'pinSplitTabButtonPref_',
       'browser.show_forward_button': 'showForwardButtonPref_',
@@ -367,7 +361,6 @@ export class SettingsAppearancePageEleme
       'side_panel.alignment_overrides': 'sidePanelAlignmentOverridesPref_',
       'side_panel.is_right_aligned': 'sidePanelIsRightAlignedPref_',
       'toolbar.pinned_actions': 'pinnedActionsPref_',
-      'helium.browser.zen_mode': 'zenModePref_',
       'webkit.webprefs.default_font_size': 'defaultFontSizePref_',
     });
 
@@ -707,17 +700,13 @@ export class SettingsAppearancePageEleme
   }
 
   protected showVerticalTabsExpandOnHoverSetting_(): boolean {
-    return this.showVerticalTabsExpandOnHoverEnabled_ &&
-        this.browserLayoutPref_?.value === BrowserLayout.VERTICAL;
+    // helium-fork: the layout is always the sidebar.
+    return this.showVerticalTabsExpandOnHoverEnabled_;
   }
 
   protected showVerticalSettings_(): boolean {
-    return this.showVerticalTabsEnabled_ &&
-        this.browserLayoutPref_?.value === BrowserLayout.VERTICAL;
-  }
-
-  protected showCenteredLocationBarSetting_(): boolean {
-    return this.browserLayoutPref_?.value !== BrowserLayout.COMPACT;
+    // helium-fork: the layout is always the sidebar.
+    return this.showVerticalTabsEnabled_;
   }
 
 
Index: src/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
===================================================================
--- src.orig/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
+++ src/chrome/browser/ui/webui/settings/settings_localized_strings_provider.cc
@@ -599,11 +599,14 @@ void AddAppearanceStrings(content::WebUI
       {"nativeFrameMaterials", IDS_SETTINGS_NATIVE_FRAME_MATERIALS},
       {"nativeFrameMaterialsDescription",
        IDS_SETTINGS_NATIVE_FRAME_MATERIALS_DESCRIPTION},
-      {"browserLayout", IDS_SETTINGS_BROWSER_LAYOUT},
-      {"browserLayoutClassic", IDS_SETTINGS_BROWSER_LAYOUT_CLASSIC},
-      {"browserLayoutDynamic", IDS_SETTINGS_BROWSER_LAYOUT_DYNAMIC},
-      {"browserLayoutCompact", IDS_SETTINGS_BROWSER_LAYOUT_COMPACT},
-      {"browserLayoutVertical", IDS_SETTINGS_BROWSER_LAYOUT_VERTICAL},
+      // helium-fork: the browser layout dropdown is gone -- there is one
+      // layout, and the address bar mode is the only thing left to pick.
+      // The IDS_SETTINGS_BROWSER_LAYOUT* strings stay defined so the
+      // upstream-facing resource file needs no edit.
+      {"addressBarMode", IDS_SETTINGS_ADDRESS_BAR_MODE},
+      {"addressBarModeDocked", IDS_SETTINGS_ADDRESS_BAR_MODE_DOCKED},
+      {"addressBarModeHidden", IDS_SETTINGS_ADDRESS_BAR_MODE_HIDDEN},
+      {"addressBarModeAuto", IDS_SETTINGS_ADDRESS_BAR_MODE_AUTO},
       {"tabStripRightAlign", IDS_SETTINGS_TAB_STRIP_RIGHT_ALIGN},
       {"browserZenMode", IDS_SETTINGS_BROWSER_ZEN_MODE},
       {"browserZenModeDescription", IDS_SETTINGS_BROWSER_ZEN_MODE_DESCRIPTION},
Index: src/chrome/browser/resources/settings/appearance_page/appearance_page.html.ts
===================================================================
--- src.orig/chrome/browser/resources/settings/appearance_page/appearance_page.html.ts
+++ src/chrome/browser/resources/settings/appearance_page/appearance_page.html.ts
@@ -63,14 +63,18 @@ export function getHtml(this: SettingsAp
         </cr-button>
       ` : ''}
     </div>
+    <!-- helium-fork: the browser has one layout - the sidebar - and the
+         only choice left is how the address bar behaves. The old
+         four-way layout dropdown and the separate frameless-mode toggle
+         (with its two pinning sub-toggles) collapsed into this. -->
     <div class="cr-row">
       <div class="flex cr-padded-text" aria-hidden="true">
-        $i18n{browserLayout}
+        $i18n{addressBarMode}
       </div>
-      <settings-dropdown-menu id="browserLayout"
-          label="$i18n{browserLayout}"
-          pref-key="helium.browser.layout"
-          .menuOptions="${this.browserLayoutOptions_}">
+      <settings-dropdown-menu id="addressBarMode"
+          label="$i18n{addressBarMode}"
+          pref-key="helium.browser.address_bar_mode"
+          .menuOptions="${this.addressBarModeOptions_}">
       </settings-dropdown-menu>
     </div>
     <div class="list-frame indented-toggles">
@@ -80,7 +84,6 @@ export function getHtml(this: SettingsAp
           label="$i18n{tabStripRightAlign}">
       </settings-toggle-button>
       <settings-toggle-button class="hr"
-          ?hidden="${!this.showCenteredLocationBarSetting_()}"
           pref-key="helium.browser.centered_location_bar"
           label="$i18n{centeredLocationBar}">
       </settings-toggle-button>
@@ -89,23 +92,6 @@ export function getHtml(this: SettingsAp
           label="$i18n{minimalLocationBar}">
       </settings-toggle-button>
     </div>
-    <settings-toggle-button class="hr"
-        pref-key="helium.browser.zen_mode"
-        label="$i18n{browserZenMode}"
-        sub-label="$i18n{browserZenModeDescription}">
-    </settings-toggle-button>
-    <div class="list-frame indented-toggles"
-        ?hidden="${!this.zenModePref_?.value}">
-      <settings-toggle-button class="hr"
-          pref-key="helium.browser.zen_mode_sidebar_pinned"
-          ?hidden="${!this.showVerticalSettings_()}"
-          label="$i18n{zenModePinSidebar}">
-      </settings-toggle-button>
-      <settings-toggle-button class="hr"
-          pref-key="helium.browser.zen_mode_top_chrome_pinned"
-          label="$i18n{zenModePinToolbar}">
-      </settings-toggle-button>
-    </div>
     <div class="hr" ?hidden="${!this.showHr_(
         this.pageVisibility_.setTheme, this.pageVisibility_.homeButton)}">
     </div>
